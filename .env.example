# PostgreSQL connection string. Railway sets this automatically when you
# attach a PostgreSQL database to this service — you don't need to set it
# by hand there. For local development you can point it at a local Postgres,
# or simply leave it unset to use the built-in in-memory fallback.
DATABASE_URL=

# Port the server listens on. Railway sets this automatically.
PORT=3000
